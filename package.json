{
  "name": "translate-image-api",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "dev": "HEADLESS=false node app.js",
    "install:clean": "rm -rf node_modules package-lock.json && npm ci",
    "docker:build": "docker build -t translate-image-api:latest .",
    "docker:run": "docker run --rm -p 3000:3000 --shm-size=1g translate-image-api:latest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "puppeteer": "^21.3.8"
  }
}
